{
  "name": "blog-v2",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "eslint --fix && vitepress build",
    "preview": "vitepress preview",
    "build:deploy": "npm run build && wrangler pages deploy .vitepress/dist --project-name=blog-v2",
    "build:deploy:preview": "npm run build && wrangler pages deploy .vitepress/dist --project-name=blog-v2 --branch=preview",
    "lint": "eslint",
    "lint:fix": "eslint --fix"
  },
  "dependencies": {
    "@lucide/vue": "^1.7.0",
    "markdown-it-textual-uml": "^0.17.1",
    "unocss": "^66.6.7",
    "vitepress": "^2.0.0-alpha.17",
    "vue": "^3.5.30"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^7.7.3",
    "@types/node": "^22.15.3",
    "@unocss/eslint-plugin": "^66.6.6",
    "@unpic/placeholder": "^0.1.2",
    "blurhash": "^2.0.5",
    "eslint": "^10.0.3",
    "sharp": "^0.33.5"
  }
}
