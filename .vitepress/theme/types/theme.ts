import type { FootbarConfig, FriendsConfig, NavConfig, ProjectsConfig } from '#types'

export interface ThemeConfig {
  nav?: NavConfig
  projects?: ProjectsConfig
  friends?: FriendsConfig
  footbar?: FootbarConfig
}
